# Sundial

A React-based web application for visualizing the position of the sun. It uses `suncalc` for calculations and `paper.js` for drawing on an HTML5 canvas. The application is built with TypeScript and communicates with a backend service using gRPC-web.
